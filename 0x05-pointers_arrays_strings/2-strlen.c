#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to evaluate
 *
 * Return: the length of the string
 */

int _strlen(char *s)

{
	int i;
	char Str[1000];

	for (i = 0; Str[i] != '\0'; ++i)

	return (i);
}
